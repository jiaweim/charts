# Grid

## 简介

`Grid` 类用于绘制网格线。主要属性：

1. xAxis
2. yAxis
3. opacity

xAxis 和 yAxis 一起定义网格线的区域。

opacity 定义网格线的透明度。

网格线分为水平和垂直两种，它们又分为三类，共有 6 种网格线：

1. h-major, v-major
2. h-medium, v-medium
3. h-minor, v-minor

可以分别设置它们 color 和 visible 属性。

